#include <twp/twp_backward_solver_lcp.h>

namespace uipc::backend::cuda
{
// Template LCP helpers are defined in twp_backward_solver_lcp.h because
// TWPBackwardSolver::solve instantiates them for unit and FEM mass policies.
}
